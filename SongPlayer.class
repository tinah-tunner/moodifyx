// Source code is decompiled from a .class file using FernFlower decompiler.
package com.moodifyx;

import java.io.File;
import java.io.IOException;
import javax.sound.sampled.AudioInputStream;
import javax.sound.sampled.AudioSystem;
import javax.sound.sampled.Clip;
import javax.sound.sampled.LineUnavailableException;
import javax.sound.sampled.UnsupportedAudioFileException;

public class SongPlayer {
   private static Clip clip;

   public SongPlayer() {
   }

   public static Clip getClip() {
      return clip;
   }

   public static void play(String var0) {
      try {
         AudioInputStream var1 = AudioSystem.getAudioInputStream(new File(var0));
         clip = AudioSystem.getClip();
         clip.open(var1);
         clip.start();
      } catch (IOException | LineUnavailableException | UnsupportedAudioFileException var2) {
         System.out.println("Error Playing Sound: " + var2.getMessage());
      }

   }

   public static void stop() {
      if (clip != null && clip.isRunning()) {
         clip.stop();
         clip.close();
      }

   }
}
